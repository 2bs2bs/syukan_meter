<div class="container mx-auto mt-8 p-8 bg-white rounded-lg shadow-md">

  <h1 class="text-3xl font-semibold mb-6">Posts#show</h1>

  <!-- Post Section -->
  <%= render 'post', post: @post %>

  <hr class="my-8 border-t border-gray-300">

  <!-- Comment Section -->
  <div class="bg-gray-100 p-6 rounded-md shadow-md mb-8 text-center">
    <% if logged_in? %>
      <%= render 'comments/form', comment: @comment, post: @post %>
    <% end %>
  </div>

  <!-- Comments List Section -->
  <!-- コメントある無しでフォームの表示非表示を行う。-->
  <div class="bg-gray-100 p-6 rounded-md shadow-md mt-6">
    <table class="w-full">
      <tbody id="table-comment">
        <%= render @comments %>
      </tbody>
    </table>
  </div>

</div>
