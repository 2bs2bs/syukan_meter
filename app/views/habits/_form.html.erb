<%= form_with(model: habit, local: true, html: { class: "max-w-md mx-auto" }) do |form| %>
  <% if habit.errors.any? %>
    <div id="error_explanation" class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 mb-4 rounded relative" role="alert">
      <strong class="font-bold">Validation Error!</strong>
      <ul>
        <% habit.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-4">
    <%= form.label :name, class: "block text-gray-700 text-sm font-bold mb-2" %>
    <%= form.text_field :name, class: "shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline", placeholder: "例)　読書" %>
  </div>

  <div class="mb-4">
    <%= form.label :description, class: "block text-gray-700 text-sm font-bold mb-2" %>
    <%= form.text_area :description, class: "shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline", placeholder: "例)　毎日読書する！" %>
  </div>

  <div class="mb-4">
    <%= form.label :start_date, class: "block text-gray-700 text-sm font-bold mb-2" %>
    <%= form.date_field :start_date, class: "shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline", value: Date.today %>
  </div>

  <div class="mb-4">
    <%= form.label :end_date, class: "block text-gray-700 text-sm font-bold mb-2" %>
    <%= form.date_field :end_date, class: "shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline", value: Date.today %>
  </div>

  <div class="mb-4">
    <%= form.label :notification_time, class: "block text-gray-700 text-sm text-sm font-bold mb-2" %>
    <%= form.number_field :notification_time, in: 0..23, class: "shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline", value: 8 %>
  </div>

  <div class="mb-4 text-center">
    <%= form.submit class: "bg-indigo-500 hover:bg-indigo-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline" %>
  </div>

<% end %>